<!-- Use this template for PRs that change the site markup, styles, or add infra. Keep short and focused. -->

## Summary
- What changed and why (one sentence):

## Checklist
- [ ] Previewed changes locally (open `index.html` or `python -m http.server`) ✅
- [ ] Validated HTML (validator.w3.org) ✅
- [ ] Run accessibility checks (Lighthouse / axe) and attached results ✅
- [ ] Confirmed `styles/main.css` import order unchanged (`variables`, `reset`, `layout`, `components`) ✅
- [ ] Included screenshots for visual changes (desktop & mobile) ✅
- [ ] Added/updated tests or documentation if adding infra or scripts ✅

## Notes
- For accessibility fixes, include the before/after screenshot and reference the failing rule (e.g., "image missing alt", "invalid role value").
- If adding JS, place scripts in `scripts/` and include with `defer` at end of `body`.

Thank you! 🎯